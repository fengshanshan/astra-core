DEEPSEEK_API_KEY=your_deepseek_api_key

# PostgreSQL - required for /chat, user registration, prompt editing.
# Local: postgresql://YOUR_USERNAME@localhost/astra
# Render: set automatically when linking a PostgreSQL database (postgres:// is auto-converted)
DATABASE_URL=postgresql://postgres:password@localhost/astra
