# 放入 /etc/nginx/conf.d/00-limits.conf
# 在 http {} 内生效（Ubuntu 默认 include conf.d/*.conf）
#
# /chat：按 IP 限制，DeepSeek 调用贵，建议偏紧（可按需改 rate）
# /api：其它接口按 IP 略放宽

limit_req_zone $binary_remote_addr zone=chat_limit:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=20r/s;
